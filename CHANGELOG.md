# Changelog

## Unreleased

- Added `xunit.analyzers` to test projects via central package management and direct project references.
- Enabled solution-wide `xUnit1051` support for test cancellation-token fixes (`TestContext.Current.CancellationToken`).
- Scoped analyzer references by excluding analyzer assets from `xunit.v3.mtp-v2` to keep analyzer ownership explicit.

